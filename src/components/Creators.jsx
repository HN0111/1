import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import { creators } from '../data/creators';
import CreatorModel from './CreatorModel';

const Creators = () => {
    return (
        <section id="creators" className="py-20 bg-transparent min-h-screen flex flex-col justify-center text-black pointer-events-none">
            <div className="container mx-auto px-6 text-center pointer-events-auto">
                {/* Content removed as requested */}
            </div>
        </section>
    );
};

export default Creators;
